# BiLSTM 재추론 평가 계획 (최종 의사결정용)

## 1. 개요 (Summary)

* `testset_total_flag.csv` 재집계 방식이 아닌 **모델 재추론(inference)** 기반으로 평가를 재수행한다.
* `output/bilstm` 내 36개 실험을 대상으로 수행한다.
* 모델 파일은 `output/bilstm/ai_models`를 사용하며, 결과는 `output/bilstm_codex`에 저장한다.
* 모든 추론 과정에 대해 `codex_scr/pot_configs.json`의 설정(data_name/pair 기준)을 이용하여 **POT 적용**
* train/test에 대한 정량 평가 및 POT 기반 정성 시각화 수행
* SMB3/SMB4 수질 항목에 대해 **2024년 전체 기간(학습 포함 구간)**에 대한 pseudo-test 추가 수행

---

## 2. 구현 변경사항 (Implementation Changes)

### 2.1 실험 목록 및 기준 정리

* `output/bilstm` 하위 36개 실험 폴더 탐색
* 실험 폴더 선택 기준:
* 하위 폴더 1개 → 해당 폴더 사용
* 여러 개 → 가장 최신 `O*` 폴더 선택
* 그 외 → 최종 수정 시간 기준 선택
* 아래 정보를 포함한 manifest 생성:
* 상위 폴더명
* 선택된 실험 폴더
* 모델 경로 (`ai_models/<top>/...pt`)
* 데이터 키 (폴더명에서 data_name/pair 파싱)
* 원천 데이터 경로 (`data_for_AI/df_univariate`)

---

### 2.2 추론 파이프라인 (train/test)

각 실험에 대해:

* 모델 로드 및 구조 복원

  (sequence length, layer 수, hidden size → 폴더명에서 파싱)

* 데이터 로드

  (`data_for_AI/df_univariate`의 train/test 원시 데이터)

* 시퀀스 재구성

  (기존 스크립트와 동일한 padding/정렬 방식 유지)

* feature별 reconstruction score 계산
* `pot_configs.json`에서 scaler 로드

  (key: data_name/pair)
  예:

  * `SMB1_AIR/solar`
  * `SMB3_WATER/O2ppm`
  * `SEA_LEVEL_LAKE/Sea_level_Repr_Lake`
* `pot_eval_stable`을 이용해 임계값 계산
* train/test에 대해 AI_POT 결과 생성

---

## 3. 평가 로직 (Evaluation Logic)

### 평가 대상

* 1QC
* AI(POT)
* 1QC + AI(POT) (OR 조건)

### 기준 라벨

* 2QC

### 평가 지표

* Recall
* F1-score

### 제외 조건 (고정)

* 원본 값이 NaN인 경우 제외
* 2QC, 1QC, AI_POT 중 하나라도 NaN이면 제외

### 판단 기준 (AI 사용 가능 여부)

<pre class="overflow-visible! px-0!" data-start="1607" data-end="1684"><div class="relative w-full mt-4 mb-1"><div class=""><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-token-bg-elevated-secondary corner-superellipse/1.1 overflow-clip rounded-3xl lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="w-full overflow-x-hidden overflow-y-auto pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼs ͼ16"><div class="cm-scroller"><div class="cm-content q9tKkq_readonly"><span>ai_usable = (recall_ai_pot > recall_1qc) AND (f1_ai_pot > f1_1qc)</span></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></pre>

---

## 4. 정성 결과 (Qualitative Outputs)

* 각 실험의 train/test에 대해 POT 기반 시각화 생성
* 함수: `plot_all_features_anomaly_with_flags`
* 저장 경로:
  <pre class="overflow-visible! px-0!" data-start="1823" data-end="1890"><div class="relative w-full mt-4 mb-1"><div class=""><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-token-bg-elevated-secondary corner-superellipse/1.1 overflow-clip rounded-3xl lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="w-full overflow-x-hidden overflow-y-auto pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼs ͼ16"><div class="cm-scroller"><div class="cm-content q9tKkq_readonly"><span>output/bilstm_codex/plots/<experiment>/<split>/fig_pot/</span></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></pre>
* 파일명은 실험명 + split 기준으로 결정

---

## 5. SMB3/SMB4 추가 pseudo-test

### 대상 변수

* temp, Salinity, pH, O2ppm, O2Per, chl-a
* 대상 스테이션: SMB3_WATER, SMB4_WATER

### 기간

* 2024-01-01 ~ 2024-12-31 (학습 포함 구간)

### 처리

* 동일한 추론 + POT + 평가 수행

### 결과 표시

<pre class="overflow-visible! px-0!" data-start="2137" data-end="2199"><div class="relative w-full mt-4 mb-1"><div class=""><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-token-bg-elevated-secondary corner-superellipse/1.1 overflow-clip rounded-3xl lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="w-full overflow-x-hidden overflow-y-auto pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼs ͼ16"><div class="cm-scroller"><div class="cm-content q9tKkq_readonly"><span>pseudo_test = True</span><br/><span>note = "학습에 포함된 기간으로 독립 검증이 아님"</span></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></pre>

---

## 6. 결과 저장 구조 (output/bilstm_codex)

### metrics

* `metrics_train_test_all.csv` (36개 실험 × train/test)
* `metrics_pseudo_test_smb3_smb4.csv`
* `missing_or_failed_experiments.csv`

---

### reports

* `evaluation_summary.md`
* 방법론 및 필터링 기준
* 실험별 정량 결과
* AI 사용 가능 여부 요약
* pseudo-test 결과 및 주의사항
* plot 경로 정리

---

### manifests

* `experiment_manifest.csv`

  (실행에 사용된 최종 매핑 정보)

---

## 7. 테스트 계획 (Test Plan)

### 사전 검증 (Dry Run)

* 2개 실험 수행:
* SMB1_AIR_solar
* SMB3_WATER_temp

검증 항목:

* POT scaler key 정상 매칭
* metric 테이블 구조 확인
* plot 생성 확인

---

### 전체 실행 검증

* 총 실험 수 = 36
* 모든 실험에 train/test 결과 존재
* pseudo-test = 12개 (6 변수 × 2 지점)
* report 내 CSV/plot 경로 정상 연결

---

### 일관성 체크

* NaN 처리 오류로 metric 누락 없음
* 모든 실험에 ai_usable 계산 완료

---

## 8. 가정 및 기본 설정 (Assumptions)

* `pot_configs.json`은 모든 36개 매핑을 포함하며, 누락 시 실패 처리 (기본값 없음)
* 모델 체크포인트는 폴더명 기반 구조와 호환됨
* 원본 데이터(`df_univariate`)가 필터링 기준의 기준 데이터
* 정성 시각화는 POT 결과만 사용 (percentile plot 없음)
